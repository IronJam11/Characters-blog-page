<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Blog</title>
  <link rel="stylesheet" href="/styles/main.css">
</head>

<body>
  <div class="container">
    <h1>
      <%= heading %>
    </h1>

    <% if (locals.post) { %>
      <form id="editForm" method="post" action="/api/posts/<%=post.id%>">
        <input type="text" name="title" value="<%=post.title %>">
        <textarea id="content" rows="10"><%=post.content %></textarea>
        <input type="text" name="Author of the book/movie" value="<%=post.author %>">
        <input type="text" name="image" value="<%=post.image%>">
        <button class="full-width" type="submit">
          <%= submit %>
        </button>
      </form>
      <% } else {%>
        <form id="newPostForm" method="post" action="/api/posts">
          <input type="text" name="title" placeholder="Name" required>
          <textarea name="content" placeholder="Description" required rows="10"></textarea>
          <input type="text" name="author" placeholder="Author of the book/movie" required>
          <input type="text" name="image" placeholder="Image URL" required>
          <button class="full-width" type="submit">
            <%= submit %>
          </button>
        </form>
        <% } %>

  </div>
</body>

</html>